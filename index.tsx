// ARQUIVO DESATIVADO PARA DEPLOY
// Este arquivo causava conflito com o App Router (app/page.tsx) durante o build do Next.js.
// Como não foi possível excluí-lo, comentamos o conteúdo para que seja ignorado.

/*
import React from 'react';
import LandingPage from './app/page';
import './app/globals.css';

export default function PreviewEntry() {
  return (
    <div className="antialiased text-zinc-50 bg-zinc-950 min-h-screen font-sans">
      <LandingPage />
    </div>
  );
}
*/

export default function Null() { return null; }
